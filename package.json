{
  "name": "catgpt-electron",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:x64": "electron-packager . CatGPT --platform=linux --arch=x64 --overwrite --icon=./CatGPTIcon.png --extra-resource=./resources",
    "build:arm64": "electron-packager . CatGPT --platform=linux --arch=arm64 --overwrite --icon=./CatGPTIcon.png --extra-resource=./resources",
    "build:all": "electron-packager . CatGPT --platform=linux --arch=x64,arm64 --overwrite --icon=./CatGPTIcon.png --extra-resource=./resources",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^32.1.2",
    "electron-packager": "^17.1.2"
  }
}
